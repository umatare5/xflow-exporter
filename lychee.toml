include_fragments = "full"
require_https = true
exclude_all_private = true
# The changelog names the tag the merge of its own release pull request creates,
# so the release link and the Unreleased comparison 404 until that merge lands.
exclude = [
  "^https://www\\.cisco\\.com/",
  "^https://github\\.com/umatare5/xflow-exporter/releases/tag/",
  "^https://github\\.com/umatare5/xflow-exporter/compare/",
]
